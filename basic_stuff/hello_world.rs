/// # A Hello World classic
///
/// This is the first of a series of basic(?) examples and studies on Rust
/// and I must remember to search the good pratices to avoid promoting a critic,
/// also, *Queen* references are everywhere.
///
/// OBS: For some reason this documentation comment is ***Markdown*** capable
///
/// Rust is a compiled so make use of a IDE or compile the necessary binaries
/// to ./bin/ using ```rustc script.rs -o ./bin/binary_name``` and call the
/// binary using ```./bin/binary_name```

fn main() {
    // Well, this is the main function, so all the statements here are executed when
    // the binary is called

    println!("Hello World !"); // Maybe I'll ready some News Of The World later
}
